import React from "react";
import styles from "./Icons.module.css";

function LogoIcon(): JSX.Element {
  return <img className={styles.logoIcon} src="logo.png" alt="logo" />;
}

export default LogoIcon;
